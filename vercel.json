{
  "version": 2,
  "buildCommand": "pnpm turbo run build --filter=@df/web",
  "outputDirectory": "apps/web/.next",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "ignoreCommand": "git diff HEAD^ HEAD --quiet ."
}
